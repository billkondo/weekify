import { colors } from '@material-ui/core';

const Pallete = {
  white: colors.grey[100],
  lightGrey: colors.grey[300],
  lightGreen: colors.lightGreen[700],
  green: colors.lightGreen[800],
};

export default Pallete;
