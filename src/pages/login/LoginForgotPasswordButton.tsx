import React from 'react';
import { Button } from '@material-ui/core';

const LoginForgotPasswordButton = () => {
  return <Button style={{ fontSize: 12 }}>Forgot Password?</Button>;
};

export default LoginForgotPasswordButton;
